apiVersion: v1
kind: Service
metadata:
  name: support-frontend
spec:
  type: NodePort
  selector:
    app: support-frontend
  ports:
    - protocol: TCP
      port: 8501
      targetPort: 8501
      nodePort: 30501
